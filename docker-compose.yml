version: '3.8'
services:
  web:
    build: .
    image: my-flask-app:latest
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - GUNICORN_CMD_ARGS=--workers 2 --bind 0.0.0.0:5000
